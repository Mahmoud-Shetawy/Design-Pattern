package solid;


public class OrderManager {

  /*  public void processOrder(Order order) {
        System.out.println("Order processed: " + order.getName()+" now....");
    }

    public void processPayment(Order order ,Payment payment) {
        System.out.println("processed payment for Order : " + order.getName());
        System.out.println("Issuing payment for amount :" + order.getPrice());

        if (payment.getType().equalsIgnoreCase("VISA")){
            System.out.println("processed VISA Card Payment ");
        }
        else if (payment.getType().equalsIgnoreCase("MASTERCARD")){
            System.out.println("processed MASTERCARD Card Payment ");
        }
        else if (payment.getType().equalsIgnoreCase("AMEX")){
            System.out.println("processed AMEX Card Payment ");
        }
        else if (payment.getType().equalsIgnoreCase("DISCOVER")){
            System.out.println("processed DISCOVER Card Payment ");
        }
    }


    void sendEmailNotification(Customer customer , String Message) {
        System.out.println("Email notification sent to " + customer.getEmail()+ "with message :" + Message);
    }*/
}
